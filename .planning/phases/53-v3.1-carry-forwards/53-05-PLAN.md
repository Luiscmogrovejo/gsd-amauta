---
plan: 53-05
phase: 53
wave: 5
depends_on: ["53-04"]
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-03, POLISH-04, POLISH-05]
files_modified:
  - .planning/REQUIREMENTS.md
files_created:
  - tests/phase-53-canary.test.cjs
---

<story>
<title>POLISH closure: v3.2 milestone-wide canary + REQUIREMENTS.md traceability cleanup</title>
<success_criteria>
- Canary asserts NO v3.2 prior-phase outputs regressed (services/module_*.py, services/party_session*.py, services/agent_schema.py, scripts/agent-compiler.cjs, migrations 021/022/023)
- Canary asserts all v3.1 protected paths still intact
- REQUIREMENTS.md traceability table closes `[ ] Pending` checkboxes for v3.1 + v3.2 requirements that have shipped
- All POLISH-01..05 fields verified end-to-end working
</success_criteria>
<doc_refs>
- .planning/phases/52-agent-compilation/52-05-PLAN.md (Phase 52 canary pattern reference)
- .planning/phases/51-party-mode-decisions-operator-cli/51-04-PLAN.md (Phase 51 canary pattern)
- .planning/REQUIREMENTS.md (traceability table — current state)
</doc_refs>
</story>

## Goal

Final closeout for Phase 53 + v3.2. Verify no prior phase outputs regressed during POLISH work; close stale traceability checkboxes (Phase 52 validator flagged these as "known stale-checkbox pattern"); document v3.2 milestone completion in SUMMARY.

## Verification Criteria

1. `node --test tests/phase-53-canary.test.cjs` exits 0; NEVER skips
2. Canary covers ALL 12+ protected paths from Phase 52 canary AND extends to Phase 52 outputs (services/agent_schema.py, scripts/agent-compiler.cjs, scripts/agent-md-to-yaml.cjs, 17 AGENT.yaml files in get-shit-done/agents/, agents/*.md byte-preservation per SC1)
3. `.planning/REQUIREMENTS.md` traceability table: rows for COMPILE-01..04, PARTY-01..04, MOD-01..04, POLISH-01..05 marked `[x]` Complete
4. Phase 53 outputs (POLISH-01..05) verified end-to-end with brief smoke tests
5. POLISH-01..05 requirements added to traceability if not already there

## must_haves

- Phase 53 canary asserts all v3.1 + v3.2 surfaces byte-preserved against PHASE_53_BASE (HEAD before Wave 1 of 53)
- Canary NEVER SKIPS (load-bearing)
- REQUIREMENTS.md traceability updated: POLISH-01..05 marked Complete; all earlier v3.2 reqs (MOD/PARTY/COMPILE) marked Complete
- Cross-reference between SUMMARY files and REQUIREMENTS.md status column passes integrity check
- Final v3.2 milestone-readiness note added to STATE.md

## Tasks

<task id="53-05-01">
  <title>Add tests/phase-53-canary.test.cjs — v3.2 milestone-wide byte-preservation canary</title>
  <agent>executor-backend</agent>
  <depends_on>[]</depends_on>
  <read_first>
    - tests/phase-52-canary.test.cjs (Phase 52 — canary pattern; reuse extractBlock helpers if exported)
    - tests/party-decisions-canary.test.cjs (Phase 51 — extractPythonBlock helper for indent-level scanning)
    - .planning/phases/53-v3.1-carry-forwards/53-CONTEXT.md (esp. Reuse map §Area 7)
  </read_first>
  <action>
    Create `tests/phase-53-canary.test.cjs` using node:test. PHASE_53_BASE is captured at start of Phase 53 Wave 1 work. Executor MUST start the file with:
```js
const PHASE_53_BASE = '32fbdc31527cb9fc5ca81db3036214054c49ae71';  // HEAD captured 2026-05-14 at Phase 53 Wave 1 dispatch
```
If commits have landed between now and Wave 5 execution that should not count as part of canary baseline, executor recaptures via `git log --format='%H' <appropriate-pre-53-commit>` and updates the constant — but the default value above is the safe baseline.

    Tests (at least 10):

    1. `phase_53_base_sha_resolves` — `git rev-parse PHASE_53_BASE` exits 0
    2. `v3_1_protected_paths_unchanged` — for each: services/skill_schema.py (excluding the new input_schema/output_schema lines POLISH-01 added — DO NOT include this path in byte-diff; instead, extract first 7 fields via Python AST and assert order unchanged), services/agent_hydrator.py, services/agent_hydrate_cli.py, services/amauta-mcp.py (excluding the 2 new MCP tools POLISH-03/04 added — same approach: extract Phase 46's 6 tool names and assert present), scripts/skill-compiler.cjs (excluding POLISH-01 validateJsonSchemaShape), bin/init.cjs (excluding POLISH-02 upgrade/uninstall), agents/* (directory glob — agents/*.md MUST be byte-identical, the SC1 lock from Phase 52 still holds)
    3. `v3_2_phase_48_outputs_unchanged` — services/module_schema.py, services/module_resolver.py, services/module_validator_cli.py: full `git diff PHASE_53_BASE -- <path>` empty
    4. `v3_2_phase_49_outputs_unchanged` — services/module_lifecycle.py, services/install_record_store.py, services/module_lifecycle_cli.py: full byte-diff empty
    5. `v3_2_phase_50_outputs_unchanged` — services/party_session.py (extract Phase 50's 8 functions only via indent-level scan), services/party_session_cli.py (extract Phase 50's 6 subparser blocks via indent-level scan), migrations/021-party-sessions.sql + DOWN: full diff empty
    6. `v3_2_phase_51_outputs_unchanged` — migrations/023-party-decisions.sql + DOWN: full diff empty; services/party_session.py post_decision/list_decisions/summarize_decisions byte-preserved (extract via indent scan)
    7. `v3_2_phase_52_outputs_unchanged` — services/agent_schema.py, scripts/agent-compiler.cjs, scripts/agent-md-to-yaml.cjs: full diff empty; 17 AGENT.yaml files in get-shit-done/agents/*/AGENT.yaml byte-identical
    8. `sc1_byte_match_still_passes` — re-run Phase 52's SC1 byte-match assertion: `node scripts/agent-compiler.cjs compile --target=claude-code --out <tmp>` → output for each of 17 agents byte-identical to current `agents/<name>.md` (the canonical SC1 lock from Phase 52 remains green)
    9. `polish_01_field_appended_not_inserted` — services/skill_schema.py: assert first 7 field declarations in EXACT Phase 43 LOCKED order, input_schema at position 8, output_schema at position 9
    10. `polish_03_04_appended_not_modified` — services/amauta-mcp.py: assert 6 Phase 46 tool names present (regression lock) + 2 new POLISH names present

    Canary NEVER skips — always runs `git diff` regardless of PG/Valkey/Node availability for the file checks. The SC1 sub-test in (8) skips with `t.skip` if `node` not on PATH; that single skip is permitted.
  </action>
  <acceptance_criteria>
    - `node --test tests/phase-53-canary.test.cjs` exits 0 with at least 10 tests
    - `grep -c "PHASE_53_BASE" tests/phase-53-canary.test.cjs >= 1`
    - `grep -c "NEVER SKIPS\|never skips\|t\.skip" tests/phase-53-canary.test.cjs` shows skip is documented as NEVER for the canary blocks
    - All 4 v3.2 prior phase outputs referenced: `grep -c "module_lifecycle\|party_session\|agent_schema\|agent-compiler" tests/phase-53-canary.test.cjs >= 4`
    - SC1 lock re-verified: `grep -c "byte-identical\|SC1" tests/phase-53-canary.test.cjs >= 1`
  </acceptance_criteria>
  <files_expected>
    modify: []
    create:
      - tests/phase-53-canary.test.cjs
    delete: []
  </files_expected>
</task>

<task id="53-05-02">
  <title>Update .planning/REQUIREMENTS.md traceability table — close v3.2 + POLISH-01..05 checkboxes</title>
  <agent>executor-general</agent>
  <depends_on>[]</depends_on>
  <read_first>
    - .planning/REQUIREMENTS.md (current traceability table; identify stale `[ ] Pending` rows)
    - .planning/phases/48..52/*-VERIFICATION.md (Phases 48-52 verification status)
    - .planning/phases/53-v3.1-carry-forwards/53-01..04-PLAN.md (Phase 53 in-progress requirements)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`:

    1. In the v3.2 traceability table, mark these requirements as Complete (change `Pending` → `Complete` in the Status column AND the checkbox to `[x]`):
       - MOD-01, MOD-02, MOD-03, MOD-04 (Phase 48/49)
       - PARTY-01, PARTY-02, PARTY-03, PARTY-04 (Phase 50/51)
       - COMPILE-01, COMPILE-02, COMPILE-03, COMPILE-04 (Phase 52)

    2. Add POLISH-01..05 to the traceability table with `Phase 53 | In Progress` status (or Complete after the rest of Phase 53 lands — but at this task's commit time they're In Progress because we're still in Wave 5).

    3. Update the v3.2 Coverage block at the end of the file:
       ```
       **Coverage:**
       - v3.2 requirements: 22 total (17 original + 5 POLISH)
       - Mapped to phases: 22 ✓
       - Unmapped: 0
       ```

    Do NOT modify any other section. Specifically PRESERVE the Out of Scope table and Future Requirements sections.
  </action>
  <acceptance_criteria>
    - `grep -c "MOD-01 | Phase 48 | Complete\|MOD-01 | Phase 48 | In Progress\|MOD-01.*Complete" .planning/REQUIREMENTS.md >= 1`
    - `grep -c "PARTY-01.*Complete" .planning/REQUIREMENTS.md >= 1`
    - `grep -c "COMPILE-01.*Complete" .planning/REQUIREMENTS.md >= 1`
    - `grep -c "POLISH-0[1-5]" .planning/REQUIREMENTS.md >= 5`
    - `grep -c "Out of Scope" .planning/REQUIREMENTS.md >= 1` (preserved)
  </acceptance_criteria>
  <files_expected>
    modify:
      - .planning/REQUIREMENTS.md
    create: []
    delete: []
  </files_expected>
</task>

<task id="53-05-03">
  <title>Phase 53 SUMMARY.md + final STATE.md + ROADMAP.md updates</title>
  <agent>executor-general</agent>
  <depends_on>["53-05-01", "53-05-02"]</depends_on>
  <read_first>
    - .planning/phases/53-v3.1-carry-forwards/53-01..04-SUMMARY.md (Wave 1-4 closeouts — synthesize)
    - .planning/STATE.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    1. Create `.planning/phases/53-v3.1-carry-forwards/53-SUMMARY.md` (phase-level, NOT a per-plan summary). Aggregate Wave 1-4 + 5 SUMMARYs into a single Phase 53 closeout. Sections: What Shipped (5 POLISH items with file references), Test Counts (sum across waves), Canary Status (10/10 pass), Open Items (any deviations surfaced), LEARNING.

    2. Update `.planning/STATE.md`:
       - Phase: 53 COMPLETE → milestone v3.2 COMPLETE
       - Progress: 100% (6/6 phases)
       - last_activity entry for Phase 53
       - milestone_status: completed

    3. Update `.planning/ROADMAP.md`:
       - Mark Phase 53 `[x]` Complete with date
       - Add v3.2 milestone status block at top: "**v3.2 The Federation** — SHIPPED 2026-05-14. 6 phases (48-53), 22 requirements, ~XXX tests."

    4. Final note in STATE: next step is `/amauta:audit-milestone v3.2` then `/amauta:complete-milestone v3.2`.
  </action>
  <acceptance_criteria>
    - `.planning/phases/53-v3.1-carry-forwards/53-SUMMARY.md` exists with at least 5 POLISH items referenced
    - `grep -c "v3.2.*COMPLETE\|v3\.2.*Complete\|v3.2.*SHIPPED" .planning/ROADMAP.md >= 1`
    - `grep -c "Phase 53.*Complete\|53.*\[x\]" .planning/ROADMAP.md >= 1`
    - `.planning/STATE.md` status field is `completed` (frontmatter)
  </acceptance_criteria>
  <files_expected>
    modify:
      - .planning/STATE.md
      - .planning/ROADMAP.md
    create:
      - .planning/phases/53-v3.1-carry-forwards/53-SUMMARY.md
    delete: []
  </files_expected>
</task>
