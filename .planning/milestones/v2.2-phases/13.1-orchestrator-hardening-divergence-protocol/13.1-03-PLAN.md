---
plan_id: 13.1-03
plan_name: HARDEN-03 + HARDEN-04 Agent .md Mechanical Updates
phase: 13.1
wave: 2
depends_on: ["13.1-01"]
autonomous: true
requirements:
  - HARDEN-03
  - HARDEN-04
files_modified:
  - agents/gsd-executor-backend.md
  - agents/gsd-executor-frontend.md
  - agents/gsd-executor-infra.md
  - agents/gsd-executor-general.md
  - agents/gsd-validator.md
execution_type: execute
---

# Plan 13.1-03 — HARDEN-03 + HARDEN-04 Agent .md Mechanical Updates

## Goal

Mechanical edits to all 4 executor agent `.md` files and the validator agent
`.md` file. No new files, no new functions — just markdown additions at
precisely the right insertion points. This plan pairs with 13.1-02 (creates
the reference file) and 13.1-04 (adds the CLI flag the validator now uses).

The .md edits are written in Wave 2 in parallel with 13.1-02 (the reference
file). Runtime Read of `divergence-protocol.md` does not fire until both land,
so the parallel write is safe.

## Scope

- All 4 executor `.md` files gain:
  1. A runtime `Read` reference to `get-shit-done/references/divergence-protocol.md`
     at the start of their RPETD protocol block.
  2. A hard rule: "if prerequisites unmet, return error, don't implement."
- `agents/gsd-validator.md` gains:
  1. Vocabulary lock: `--pass` / `--fail` / `--gaps-found` language (no
     improvisation).
  2. Never-invent-requirement-IDs rule: "Finding without req ID = fail or
     non_gaps_observations."
  3. Divergence report pre-gate scan: reads
     `.planning/milestones/<phase>/divergence-reports/` before evaluating
     gates; unresolved report = automatic `gaps_found` floor.

## Must-Haves

- [ ] All 4 executor .md files reference `divergence-protocol.md`.
- [ ] All 4 executor .md files contain the "return error, don't implement"
      rule.
- [ ] `gsd-validator.md` contains the three vocabulary flags by name.
- [ ] `gsd-validator.md` contains the never-invent-req-IDs rule.
- [ ] `gsd-validator.md` references the divergence-reports directory as a
      pre-gate scan.

## Tasks

<task id="13.1-03-01">
  <title>Add divergence protocol reference + prereq-unmet rule to gsd-executor-backend.md</title>
  <agent>executor-general</agent>
  <depends_on>[]</depends_on>
  <read_first>
    - agents/gsd-executor-backend.md (full file, 196 lines per CONTEXT.md; find the existing runtime Read section — look for `cli-variables.md` or `pre-execution-checklist.md` references)
    - .planning/milestones/v2.2-phases/13.1-orchestrator-hardening-divergence-protocol/13.1-CONTEXT.md (HARDEN-03 decisions)
  </read_first>
  <action>
    In `agents/gsd-executor-backend.md`, add two things:

    1. **Runtime Read reference** — Find the existing runtime Read list (typically a block near the top that already reads `cli-variables.md` and `pre-execution-checklist.md`). Add a new bullet:
       ```
       - Read `get-shit-done/references/divergence-protocol.md` at the start of every task, before touching any file. If observed state contradicts the task brief, follow the divergence protocol — do NOT silently adjust.
       ```
       If there is no existing runtime Read list, add it as a new section `<runtime_read>` immediately after the `<role>` block.

    2. **Hard rule block** — Add a new `<prerequisites_hard_rule>` section (or append to an existing `<constraints>` section if one exists) with exactly this content:
       ```
       <prerequisites_hard_rule>
       If any prerequisite for this task is unmet (missing file, stale state, contradictory assumption), you MUST:
       1. Stop immediately.
       2. Write a divergence_report per `get-shit-done/references/divergence-protocol.md`.
       3. Return an error to the orchestrator. Exit non-zero.

       You are FORBIDDEN from:
       - Implementing "what the task probably meant"
       - Fixing the prerequisite inline and continuing
       - Committing partial work to "show progress"
       - Silently adjusting the manifest

       If prerequisites unmet: return error, don't implement.
       </prerequisites_hard_rule>
       ```

    Do not remove or reorder any existing section. Append-only.
  </action>
  <acceptance_criteria>
    - `grep -c "divergence-protocol.md" agents/gsd-executor-backend.md` returns >= 1.
    - `grep -c "prerequisites_hard_rule" agents/gsd-executor-backend.md` returns 2 (open + close tag).
    - `grep -c "return error, don't implement" agents/gsd-executor-backend.md` returns >= 1.
    - `grep -c "divergence_report" agents/gsd-executor-backend.md` returns >= 1.
    - File line count increases by >= 15 vs pre-edit baseline (sanity check — section is non-trivial).
  </acceptance_criteria>
  <files_expected>
    modify:
      - agents/gsd-executor-backend.md
    create: []
    delete: []
  </files_expected>
</task>

<task id="13.1-03-02">
  <title>Add divergence protocol reference + prereq-unmet rule to gsd-executor-frontend.md</title>
  <agent>executor-general</agent>
  <depends_on>[]</depends_on>
  <read_first>
    - agents/gsd-executor-frontend.md (full file, find the existing runtime Read section)
    - agents/gsd-executor-backend.md (read the edit landed in 13.1-03-01 to mirror structure exactly)
    - .planning/milestones/v2.2-phases/13.1-orchestrator-hardening-divergence-protocol/13.1-CONTEXT.md (HARDEN-03 decisions)
  </read_first>
  <action>
    Apply the SAME two edits as 13.1-03-01 to `agents/gsd-executor-frontend.md`:

    1. Add runtime Read bullet:
       ```
       - Read `get-shit-done/references/divergence-protocol.md` at the start of every task, before touching any file. If observed state contradicts the task brief, follow the divergence protocol — do NOT silently adjust.
       ```

    2. Append the `<prerequisites_hard_rule>` section with identical content:
       ```
       <prerequisites_hard_rule>
       If any prerequisite for this task is unmet (missing file, stale state, contradictory assumption), you MUST:
       1. Stop immediately.
       2. Write a divergence_report per `get-shit-done/references/divergence-protocol.md`.
       3. Return an error to the orchestrator. Exit non-zero.

       You are FORBIDDEN from:
       - Implementing "what the task probably meant"
       - Fixing the prerequisite inline and continuing
       - Committing partial work to "show progress"
       - Silently adjusting the manifest

       If prerequisites unmet: return error, don't implement.
       </prerequisites_hard_rule>
       ```

    Mirror the exact placement decided in 13.1-03-01 (runtime Read goes near existing `cli-variables.md` reference; hard-rule block goes at end of `<constraints>` if present, else as a new top-level section).
  </action>
  <acceptance_criteria>
    - `grep -c "divergence-protocol.md" agents/gsd-executor-frontend.md` returns >= 1.
    - `grep -c "prerequisites_hard_rule" agents/gsd-executor-frontend.md` returns 2.
    - `grep -c "return error, don't implement" agents/gsd-executor-frontend.md` returns >= 1.
    - `grep -c "divergence_report" agents/gsd-executor-frontend.md` returns >= 1.
    - File line count increases by >= 15 vs pre-edit baseline (identical shape to 13.1-03-01; capture `wc -l agents/gsd-executor-frontend.md` before the edit and assert the post-edit count minus the baseline is >= 15 — sanity check that the section is non-trivial).
  </acceptance_criteria>
  <files_expected>
    modify:
      - agents/gsd-executor-frontend.md
    create: []
    delete: []
  </files_expected>
</task>

<task id="13.1-03-03">
  <title>Add divergence protocol reference + prereq-unmet rule to gsd-executor-infra.md</title>
  <agent>executor-general</agent>
  <depends_on>[]</depends_on>
  <read_first>
    - agents/gsd-executor-infra.md (full file)
    - agents/gsd-executor-backend.md (read the edit landed in 13.1-03-01 to mirror structure exactly)
    - .planning/milestones/v2.2-phases/13.1-orchestrator-hardening-divergence-protocol/13.1-CONTEXT.md (HARDEN-03 decisions)
  </read_first>
  <action>
    Apply the SAME two edits as 13.1-03-01 to `agents/gsd-executor-infra.md`:

    1. Runtime Read bullet referencing `get-shit-done/references/divergence-protocol.md`.
    2. `<prerequisites_hard_rule>` section with identical body (1. Stop immediately. 2. Write a divergence_report. 3. Return an error. FORBIDDEN list. "return error, don't implement.").

    Mirror placement from 13.1-03-01.
  </action>
  <acceptance_criteria>
    - `grep -c "divergence-protocol.md" agents/gsd-executor-infra.md` returns >= 1.
    - `grep -c "prerequisites_hard_rule" agents/gsd-executor-infra.md` returns 2.
    - `grep -c "return error, don't implement" agents/gsd-executor-infra.md` returns >= 1.
    - `grep -c "divergence_report" agents/gsd-executor-infra.md` returns >= 1.
    - File line count increases by >= 15 vs pre-edit baseline (identical shape to 13.1-03-01; capture `wc -l agents/gsd-executor-infra.md` before the edit and assert the post-edit count minus the baseline is >= 15 — sanity check that the section is non-trivial).
  </acceptance_criteria>
  <files_expected>
    modify:
      - agents/gsd-executor-infra.md
    create: []
    delete: []
  </files_expected>
</task>

<task id="13.1-03-04">
  <title>Add divergence protocol reference + prereq-unmet rule to gsd-executor-general.md</title>
  <agent>executor-general</agent>
  <depends_on>[]</depends_on>
  <read_first>
    - agents/gsd-executor-general.md (full file, 176 lines per CONTEXT.md)
    - agents/gsd-executor-backend.md (read the edit landed in 13.1-03-01 to mirror structure exactly)
    - .planning/milestones/v2.2-phases/13.1-orchestrator-hardening-divergence-protocol/13.1-CONTEXT.md (HARDEN-03 decisions)
  </read_first>
  <action>
    Apply the SAME two edits as 13.1-03-01 to `agents/gsd-executor-general.md`:

    1. Runtime Read bullet referencing `get-shit-done/references/divergence-protocol.md`.
    2. `<prerequisites_hard_rule>` section with identical body.

    Mirror placement from 13.1-03-01.
  </action>
  <acceptance_criteria>
    - `grep -c "divergence-protocol.md" agents/gsd-executor-general.md` returns >= 1.
    - `grep -c "prerequisites_hard_rule" agents/gsd-executor-general.md` returns 2.
    - `grep -c "return error, don't implement" agents/gsd-executor-general.md` returns >= 1.
    - `grep -c "divergence_report" agents/gsd-executor-general.md` returns >= 1.
    - File line count increases by >= 15 vs pre-edit baseline (identical shape to 13.1-03-01; capture `wc -l agents/gsd-executor-general.md` before the edit and assert the post-edit count minus the baseline is >= 15 — sanity check that the section is non-trivial).
  </acceptance_criteria>
  <files_expected>
    modify:
      - agents/gsd-executor-general.md
    create: []
    delete: []
  </files_expected>
</task>

<task id="13.1-03-05">
  <title>Add vocabulary lock + never-invent-req-IDs rule + divergence-reports pre-gate scan to gsd-validator.md</title>
  <agent>executor-general</agent>
  <depends_on>[]</depends_on>
  <read_first>
    - agents/gsd-validator.md (full file, 236 lines per CONTEXT.md — locate the 4 existing quality gates and the current `--pass`/`--fail` verdict logic)
    - .planning/milestones/v2.2-phases/13.1-orchestrator-hardening-divergence-protocol/13.1-CONTEXT.md (HARDEN-04 validator decisions — 3 flags, gaps-report schema, orchestrator routing)
    - .planning/REQUIREMENTS.md (confirm HARDEN-03 wording matches the agent-file edit)
  </read_first>
  <action>
    Edit `agents/gsd-validator.md` to add three hardening blocks. No section removals — additive only.

    1. **Vocabulary lock section** — Append a new `<vocabulary_lock>` section near the existing verdict protocol. Content:
       ```
       <vocabulary_lock>
       The validator emits exactly one of three verdicts, via one of three CLI flags — no improvisation:

       - `--pass` (exit 0) → phase advances, roadmap updated.
       - `--gaps-found` (exit 2) → phase does NOT advance. Gaps report written. Orchestrator reroutes to re-planning (delta plan). Mini-wave + re-validation. No retry counter.
       - `--fail` (exit 1) → phase does NOT advance. Fatal. Roadmapper invoked. Human escalation. No auto-retry.

       You are FORBIDDEN from:
       - Inventing new verdict words ("mostly pass", "conditional pass", "retry", "warn-only").
       - Extending the JSON verdict with new fields.
       - Tagging gaps with severity levels — all gaps in `gaps-report-<timestamp>.json` are equal; severity tagging is forbidden.

       Recoverable gate failure ("fix without redesign") → `--gaps-found`.
       Non-recoverable gate failure → `--fail`.
       Pass → all 5 gates clean AND no unresolved divergence reports.
       </vocabulary_lock>
       ```

    2. **Never-invent-requirement-IDs rule** — Append a new `<requirement_id_rule>` section:
       ```
       <requirement_id_rule>
       The validator NEVER invents a requirement ID. Every finding must:

       - Cite an existing `requirement_id` (e.g., `HARDEN-01`, `CREATIVE-03`) from `.planning/REQUIREMENTS.md`, OR
       - Be filed under `non_gaps_observations[]` in the gaps report (the pressure-release valve for genuinely cosmetic findings).

       A finding without a requirement ID that is NOT under `non_gaps_observations` = validator error. Fail the phase OR reroute to `non_gaps_observations`. Never coin a new ID.

       "Should be a requirement" = file a divergence_report (verdict_ambiguity), do NOT add a new requirement inline.
       </requirement_id_rule>
       ```

    3. **Divergence report pre-gate scan** — Append a new `<divergence_pre_gate_scan>` section:
       ```
       <divergence_pre_gate_scan>
       BEFORE evaluating any of the 5 quality gates, scan `.planning/milestones/<phase>/divergence-reports/` for any report missing an `orchestrator_response` field.

       Protocol:
       1. List all `*.json` files in `.planning/milestones/<phase>/divergence-reports/`.
       2. For each, parse the JSON. If `orchestrator_response` is absent or null → report is UNRESOLVED.
       3. If any unresolved reports exist, the minimum verdict floor is `--gaps-found`. Gate evaluation continues but cannot escape gaps_found upward.
       4. List each unresolved report's `task_id` and `divergence_type` in the gaps-report under `non_gaps_observations[]` with prefix `unresolved_divergence:`.
       5. This scan is MANDATORY and runs before any other check. Skipping it = validator fail.

       Read `get-shit-done/references/divergence-protocol.md` for the full schema. You share the schema with the 4 executors.
       </divergence_pre_gate_scan>
       ```

    Preserve the existing 5-gate structure, the pass/fail CLI flags already documented, and all other prose. Additive only.
  </action>
  <acceptance_criteria>
    - `grep -c "vocabulary_lock" agents/gsd-validator.md` returns 2 (open + close).
    - `grep -c "requirement_id_rule" agents/gsd-validator.md` returns 2.
    - `grep -c "divergence_pre_gate_scan" agents/gsd-validator.md` returns 2.
    - `grep -c -- "--gaps-found" agents/gsd-validator.md` returns >= 2.
    - `grep -c -- "--pass" agents/gsd-validator.md` returns >= 2.
    - `grep -c -- "--fail" agents/gsd-validator.md` returns >= 2.
    - `grep -c "divergence-reports/" agents/gsd-validator.md` returns >= 1.
    - `grep -c "NEVER invents" agents/gsd-validator.md` returns 1.
    - `grep -c "non_gaps_observations" agents/gsd-validator.md` returns >= 2.
    - `grep -c "divergence-protocol.md" agents/gsd-validator.md` returns >= 1.
  </acceptance_criteria>
  <files_expected>
    modify:
      - agents/gsd-validator.md
    create: []
    delete: []
  </files_expected>
</task>

## Verification

- All 4 executor .md files reference `divergence-protocol.md`.
- All 4 executor .md files contain `prerequisites_hard_rule` tags.
- `gsd-validator.md` contains all three new sections by name.
- `gsd-validator.md` mentions `--gaps-found` at least twice.
- Every file still parses as valid markdown (no accidental frontmatter break).

## Risks

1. **Agent .md line count budget** — CONTEXT.md does not specify a line cap for
   this phase, but v2.6 prompt-size budget says NEW agents ≤ 200 lines;
   existing agents may grow +25%. Backend is 196, general is 176, validator is
   236. Append only ~15-20 lines per file; verify totals stay within +25%
   budget. If any file would exceed, raise a divergence report.
2. **Runtime Read placement consistency** — All 4 executors must place the new
   Read bullet in the SAME structural location (e.g., top of RPETD protocol,
   not scattered). Task 13.1-03-01 is the template; 02/03/04 mirror.
3. **Validator section ordering** — The three new sections must not break the
   existing 5-gate flow. Append them AFTER the gate list, not inside it.

## Notes

- Depends on 13.1-01 only (not on 13.1-02) — the agent .md files reference the
  protocol file by path; the Read tool resolves at runtime. Both plans can
  land in parallel; runtime Read only fires in 13.1-05 behavioral test.
- This plan is mechanical. No judgment calls on content — the exact text is in
  the action blocks. Deviation from the specified text = divergence report.
- HARDEN-04 overlap: this plan includes the VALIDATOR .md vocabulary lock
  (because it is mechanical). The CLI-flag implementation lives in 13.1-04.
