---
plan_id: 14-04
plan_name: Agent Wiring + Workflow Integration
phase: 14
wave: 4
depends_on:
  - 14-02
  - 14-03
autonomous: true
requirements:
  - PLAN-04
  - PLAN-06
  - PLAN-07
files_modified:
  - agents/gsd-operator.md
  - agents/gsd-validator.md
  - get-shit-done/workflows/plan-phase.md
  - get-shit-done/workflows/execute-phase.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
execution_type: execute
---

# Plan 14-04 — Agent Wiring + Workflow Integration

## Goal

Wire the fully-implemented `plan-to-tasks` into the agent and workflow layer:
add the `PLAN_REGISTRATION` parser to gsd-operator.md, add the structural
advisory to gsd-validator.md, extend plan-phase.md's checker quality gate with
PLAN-06 checks, replace execute-phase.md's thin registration loop with
plan-to-tasks for phases >= 14, and document the cutoff in STATE.md.

## Scope

- `agents/gsd-operator.md`: new `<plan_registration_phase_end>` hand-wired
  parser section.
- `agents/gsd-validator.md`: new structural advisory check for
  `PLAN_REGISTRATION` block presence.
- `get-shit-done/workflows/plan-phase.md`: extend plan-checker quality gate
  with PLAN-06 checks (no cycle, task count <= 10, every task has `<agent>`,
  advisory zero-deps warning, mandatory `<story>` block).
- `get-shit-done/workflows/execute-phase.md`: replace the thin amauta task
  registration loop (lines 120-168) with `plan-to-tasks` call for phases >= 14.
- `.planning/STATE.md`: add cutoff note to "Roadmap Evolution" section.

## Must-Haves

- [ ] `gsd-operator.md` has a `<plan_registration_phase_end>` section following
      the same hand-wired pattern as `<qa_report_phase_end>`.
- [ ] `gsd-validator.md` has a structural advisory for PLAN_REGISTRATION.
- [ ] `plan-phase.md` quality gate checks task count <= 10, every task has
      `<agent>`, mandatory `<story>` block.
- [ ] `plan-phase.md` has advisory zero-deps warning for waves with > 2 tasks
      and zero `<depends_on>` edges.
- [ ] `execute-phase.md` calls `gsd-tools.cjs plan-to-tasks` for phases >= 14.
- [ ] `execute-phase.md` preserves the existing thin registration loop for
      phases 9-13.
- [ ] STATE.md "Roadmap Evolution" has the cutoff documentation line.
- [ ] REQUIREMENTS.md PLAN-04 errata applied: implicit-N+1 text struck through,
      explicit-only replacement text added, PITFALLS P8 footnote present.

## Verification

```bash
# Operator has the new parser section
grep 'plan_registration_phase_end' agents/gsd-operator.md
# Validator has the advisory
grep 'PLAN_REGISTRATION' agents/gsd-validator.md
# Plan-phase has the extended quality gate
grep 'task count' get-shit-done/workflows/plan-phase.md || grep '<story>' get-shit-done/workflows/plan-phase.md
# Execute-phase has the plan-to-tasks call gated by phase number
grep 'plan-to-tasks' get-shit-done/workflows/execute-phase.md
# STATE.md has the cutoff line
grep 'plan-to-tasks' .planning/STATE.md
# PLAN-04 errata applied
grep 'explicit-only' .planning/REQUIREMENTS.md
grep 'PITFALLS P8' .planning/REQUIREMENTS.md
```

## Tasks

<story>
  <title>Phase 14 Wave 4: Agent Wiring and Workflow Integration</title>
  <success_criteria>
    Given planToTasks() is fully implemented with Pass 0-2 (Waves 2-3),
    When Wave 4 completes,
    Then gsd-operator.md parses PLAN_REGISTRATION blocks via a hand-wired section,
    And gsd-validator.md checks for PLAN_REGISTRATION structural presence as an advisory,
    And plan-phase.md quality gate rejects plans violating PLAN-06 criteria,
    And execute-phase.md invokes plan-to-tasks for phases >= 14 while preserving the legacy loop for 9-13.
  </success_criteria>
  <doc_refs>
    - .planning/milestones/v2.2-phases/14-p-phase-task-management-integration/14-CONTEXT.md
    - .planning/milestones/v2.2-phases/14-p-phase-task-management-integration/14-RESEARCH.md
  </doc_refs>
</story>

<task id="14-04-01">
  <title>Add PLAN_REGISTRATION parser to gsd-operator.md and structural advisory to gsd-validator.md</title>
  <agent>executor-backend</agent>
  <depends_on>[]</depends_on>
  <read_first>
    - agents/gsd-operator.md lines 397-413 (the qa_report_phase_end section — the hand-wired pattern to follow exactly: XML-tagged section with bash snippet that greps task content)
    - agents/gsd-operator.md lines 363-380 (d_phase_structured_learning section — another hand-wired parser example)
    - agents/gsd-operator.md lines 382-395 (applied_learning_citation_scan — the third existing parser)
    - agents/gsd-validator.md lines 192-218 (Spec Inheritance + QA Advisory section — the structural advisory pattern: check presence, log to task notes, kill-switch gating)
    - .planning/milestones/v2.2-phases/14-p-phase-task-management-integration/14-CONTEXT.md (Area 4 — PLAN_REGISTRATION block schema with fields: plan_id, story_id, task_count, cap, task_ids, agent_assignments, edges, dag_text, inherited_criteria_count; 1500 char cap; hand-wired parser; validator checks structural presence only)
  </read_first>
  <action>
    **Preflight:** Verify `get-shit-done/references/divergence-protocol.md` contains `version: "1.1.0"` (Wave 1 dependency). If not, halt — Wave 1 must complete first.

    **Part A: gsd-operator.md — add `<plan_registration_phase_end>` section**

    Insert a new XML-tagged section AFTER `</qa_report_phase_end>` (after line 413) and BEFORE `<execution_type_classification>` (line 415). Follow the exact hand-wired pattern:

    ```xml
    <plan_registration_phase_end>
    ### Post-Task: PLAN_REGISTRATION Summary (Phase 14)

    After RPETD phases are logged, grep P-phase content for the `PLAN_REGISTRATION:` block. Surface at phase-end alongside LEARNING/APPLIED_LEARNING/QA_REPORT. The block has a 1500-char budget (500 extra chars vs T-phase for the dag_text field).

    ```bash
    # Extract PLAN_REGISTRATION from P-phase content (Phase 14 PLAN-07)
    PLAN_REG_BLOCK=$(printf '%s' "$TASK_CONTENT" | grep -A 20 '^PLAN_REGISTRATION:' | head -25 || echo "")
    if [ -n "$PLAN_REG_BLOCK" ]; then
      PLAN_ID=$(printf '%s' "$PLAN_REG_BLOCK" | grep -oE 'plan_id: [^ ]+' | head -1 || echo "")
      TASK_COUNT=$(printf '%s' "$PLAN_REG_BLOCK" | grep -oE 'task_count: [0-9]+' | head -1 || echo "")
      STORY_ID=$(printf '%s' "$PLAN_REG_BLOCK" | grep -oE 'story_id: ST-[0-9]+' | head -1 || echo "")
      printf '[PLAN_REG] %s | %s | %s\n' "$PLAN_ID" "$TASK_COUNT" "$STORY_ID"
    fi
    ```

    **Non-code tasks:** PLAN_REGISTRATION only appears on P-phase tasks. Non-plan tasks have no PLAN_REGISTRATION line (both are valid).

    **Phase-end summary:** When completing a phase, aggregate PLAN_REGISTRATION blocks from all plan tasks for a registration health summary (e.g., "4 plans registered, 28 tasks total, 0 registration failures").
    </plan_registration_phase_end>
    ```

    **Part B: gsd-validator.md — add PLAN_REGISTRATION structural advisory**

    Insert a new advisory section AFTER the "Spec Inheritance + QA Advisory" section (after line 218) and BEFORE the next section. Follow the exact advisory pattern:

    ```markdown
    ### Plan Registration Advisory (Phase 14 -- not a numbered gate)

    After gates pass, check validation output for `[ADVISORY] PLAN_REGISTRATION`. This advisory is informational in v2.6 -- it does NOT block validation.

    **When you see the advisory:**
    ```bash
    # Log advisory to task notes (visible in amauta show)
    $CLI note TK-XXXX --text "[ADVISORY] P-phase PLAN_REGISTRATION block absent -- plan-to-tasks may not have run" --agent validator
    ```

    **Structural checks (presence only, NOT content validation):**
    - `PLAN_REGISTRATION:` -- block is present
    - `plan_id:` -- non-empty
    - `task_count:` -- non-empty, numeric
    - `story_id:` -- non-empty, matches ST-[0-9]+ pattern

    **When advisory does NOT fire:** No action needed. PLAN_REGISTRATION block is structurally present.

    **Kill switch:** `GSD_P_AUTO_TASK=false` disables the advisory entirely (plan-to-tasks was not invoked).

    **Non-plan tasks:** Advisory does not fire for non-plan task types.
    ```

    The validator does NOT re-validate the dependency graph, recompute agent assignments, or check inherited criteria counts. Pass 0 in plan-to-tasks is the authority.
  </action>
  <acceptance_criteria>
    - grep -c 'plan_registration_phase_end' agents/gsd-operator.md returns exactly 2 (open + close tags)
    - grep 'PLAN_REGISTRATION' agents/gsd-operator.md returns at least 2 matches (section header + grep command)
    - grep 'plan_id.*task_count.*story_id' agents/gsd-operator.md returns a match (the printf summary line)
    - grep -c 'PLAN_REGISTRATION' agents/gsd-validator.md returns at least 3 (section header + advisory text + structural check)
    - grep 'GSD_P_AUTO_TASK' agents/gsd-validator.md returns a match (kill switch note)
    - The operator section appears AFTER qa_report_phase_end and BEFORE execution_type_classification (verify with grep -n)
    - The validator section appears AFTER "Spec Inheritance" section (verify with grep -n)
  </acceptance_criteria>
  <files_expected>
    modify:
      - agents/gsd-operator.md
      - agents/gsd-validator.md
    create: []
    delete: []
  </files_expected>
</task>

<task id="14-04-02">
  <title>Extend plan-phase.md checker quality gate with PLAN-06 checks</title>
  <agent>executor-backend</agent>
  <depends_on>[]</depends_on>
  <read_first>
    - get-shit-done/workflows/plan-phase.md lines 394-404 (the existing quality_gate checklist — this is the section to extend)
    - get-shit-done/workflows/plan-phase.md lines 1-50 (file header and overall structure — understand where the quality gate fits in the workflow)
    - get-shit-done/references/plan-task-xml-schema.md (the schema definition created in 14-01-02 — the checker must verify plans comply with this schema; PREFLIGHT: verify file exists before reading, hard error if missing since this is a Wave 1 dependency)
    - .planning/milestones/v2.2-phases/14-p-phase-task-management-integration/14-CONTEXT.md (Area 2 — plan-checker stays focused on content quality, does NOT duplicate cycle/cap checks from Pass 0; advisory zero-deps warning for waves with > 2 tasks and 0 depends_on edges)
  </read_first>
  <action>
    Extend the `<quality_gate>` checklist in `get-shit-done/workflows/plan-phase.md` (around line 394) with Phase 14 PLAN-06 checks. Add these items to the existing checklist:

    **New mandatory checklist items (for phases >= 14):**
    ```
    - [ ] `<story>` block present at top of plan (mandatory for phases >= 14, per plan-task-xml-schema.md)
    - [ ] Every `<task>` has a non-empty `<agent>` field (executor-backend, executor-frontend, executor-infra, or executor-general)
    - [ ] Every `<task>` has a `<files_expected>` block with modify/create/delete sublists (HARDEN-01 mandate)
    - [ ] Task count per plan does not exceed 10 (PLAN-05 cap)
    - [ ] Each plan has `requirements` field in frontmatter listing covered PLAN-XX IDs
    ```

    **New advisory check (not a hard gate):**
    Add a paragraph AFTER the quality_gate checklist explaining the advisory zero-deps warning:

    ```
    **Advisory: Zero-dependency warning (Phase 14+):**
    If a wave contains more than 2 tasks and NONE of them declare `<depends_on>` edges (all are `[]`), emit an advisory warning: "Wave N has {count} tasks with zero explicit dependencies — verify that all tasks are truly parallel-eligible (PITFALLS P8)." This is an advisory, not a hard gate — genuinely parallel tasks are valid.
    ```

    **What the plan-checker does NOT check (scope boundary):**
    Add a note below the advisory:
    ```
    **NOTE:** The plan-checker does NOT validate DAG cycles or run the cap-overflow split algorithm — those belong to `plan-to-tasks` Pass 0 (runtime validation, not plan-time). The checker focuses on content quality: read_first completeness, action concreteness, acceptance criteria verifiability, schema compliance.
    ```

    This maintains the CONTEXT.md decision that "plan-checker stays focused on plan content quality and does NOT duplicate graph logic."
  </action>
  <acceptance_criteria>
    - grep -c '<story>' get-shit-done/workflows/plan-phase.md returns at least 1 (the new checklist item)
    - grep -c '<agent>' get-shit-done/workflows/plan-phase.md returns at least 1 (the agent field check)
    - grep -c 'files_expected' get-shit-done/workflows/plan-phase.md returns at least 1 (the HARDEN-01 mandate check)
    - grep -c 'exceed 10\|does not exceed' get-shit-done/workflows/plan-phase.md returns at least 1 (cap check)
    - grep 'Zero-dependency warning\|zero-deps\|zero explicit dependencies' get-shit-done/workflows/plan-phase.md returns a match (advisory)
    - grep 'PITFALLS P8' get-shit-done/workflows/plan-phase.md returns a match (footnote reference)
    - grep -c 'does NOT validate DAG cycles\|does NOT validate.*cycle' get-shit-done/workflows/plan-phase.md returns at least 1 (scope boundary note)
    - The existing quality_gate items (read_first, acceptance_criteria, etc.) are still present (verify with grep)
  </acceptance_criteria>
  <files_expected>
    modify:
      - get-shit-done/workflows/plan-phase.md
    create: []
    delete: []
  </files_expected>
</task>

<task id="14-04-03">
  <title>Replace execute-phase.md thin registration with plan-to-tasks for phases >= 14 + PLAN-04 errata</title>
  <agent>executor-backend</agent>
  <depends_on>["14-04-01", "14-04-02"]</depends_on>
  <read_first>
    - get-shit-done/workflows/execute-phase.md lines 92-171 (discover_and_group_plans step — the entire section including the existing thin registration loop at lines 120-168 that Phase 14 REPLACES for phases >= 14)
    - get-shit-done/workflows/execute-phase.md lines 330-340 (manifest-check migration note — HARDEN-01 grandfathering pattern to follow for plan-to-tasks)
    - get-shit-done/bin/gsd-tools.cjs (planToTasks CLI dispatch — `case 'plan-to-tasks':` — verify the exact CLI invocation format)
    - .planning/milestones/v2.2-phases/14-p-phase-task-management-integration/14-CONTEXT.md (Area 1 "Phase cutoff" — hard cutoff at phase >= 14, phase number authoritative, not <story> block presence; Area 3 "Kill switch" — GSD_P_AUTO_TASK=false two-layer check)
    - .planning/STATE.md (read the "Roadmap Evolution" section to find the insertion point for the cutoff documentation line)
    - .planning/REQUIREMENTS.md (find PLAN-04 dependency ordering requirement — the implicit-N+1 text that needs errata strikethrough + explicit-only replacement + PITFALLS P8 footnote)
  </read_first>
  <action>
    **Part A: execute-phase.md — phase-gated plan-to-tasks invocation**

    In the `discover_and_group_plans` step, AFTER the PLAN_INDEX load (around line 97) and BEFORE the existing thin registration loop (line 120), insert a phase-number check and plan-to-tasks invocation block:

    ```bash
    # Phase-gated plan-to-tasks registration (Phase 14+)
    # For phases >= 14, plan-to-tasks handles full task registration with metadata,
    # dedup bypass, agent conflict detection, and dependency linking.
    # For phases 9-13, the legacy thin registration loop below handles it.
    PHASE_NUM_FLOAT=$(echo "$PHASE_NUMBER" | python3 -c "import sys; parts=sys.stdin.read().strip().split('.'); print(float(parts[0]))" 2>/dev/null || echo "0")

    if python3 -c "exit(0 if ${PHASE_NUM_FLOAT} >= 14 else 1)" 2>/dev/null; then
      # Phase >= 14: use plan-to-tasks (or skip entirely if kill switch is active)
      if [ "${GSD_P_AUTO_TASK:-false}" = "false" ]; then
        echo "[plan-to-tasks] Phase ${PHASE_NUMBER} >= 14 but GSD_P_AUTO_TASK=false — skipping task registration entirely (no legacy loop fallback for phase 14+)"
      else
      echo "[plan-to-tasks] Phase ${PHASE_NUMBER} >= 14 — running plan-to-tasks for full registration"
      TOOLS="$HOME/.claude/get-shit-done/bin/gsd-tools.cjs"
      P2T_FAILURES=0

      for plan_file in ${PHASE_DIR}/*-PLAN.md; do
        [ -f "$plan_file" ] || continue
        # Skip plans that already have a SUMMARY.md (completed plans)
        PLAN_BASE=$(basename "$plan_file" -PLAN.md)
        [ -f "${PHASE_DIR}/${PLAN_BASE}-SUMMARY.md" ] && continue

        echo "[plan-to-tasks] Registering: $plan_file"
        P2T_RESULT=$(node "$TOOLS" plan-to-tasks "$plan_file" 2>&1) || {
          echo "[plan-to-tasks] FAILED: $plan_file"
          echo "$P2T_RESULT"
          P2T_FAILURES=$((P2T_FAILURES + 1))
          continue
        }

        # Extract story and task IDs from result for executor spawning
        P2T_STORY=$(echo "$P2T_RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin).get('story_id',''))" 2>/dev/null || echo "")
        P2T_TASKS=$(echo "$P2T_RESULT" | python3 -c "import sys,json; d=json.load(sys.stdin); [print(tid) for tid in d.get('tasks_created',[])]" 2>/dev/null || echo "")

        if [ -n "$P2T_STORY" ]; then
          PHASE_STORY="$P2T_STORY"
        fi

        # Store task IDs for executor spawning
        for tid in $P2T_TASKS; do
          eval "PLAN_TASK_${PLAN_BASE//-/_}=$tid"
        done
        echo "[plan-to-tasks] Registered: $plan_file -> Story: $P2T_STORY, Tasks: $(echo $P2T_TASKS | wc -w | tr -d ' ')"
      done

      if [ "$P2T_FAILURES" -gt 0 ]; then
        echo "[plan-to-tasks] WARNING: $P2T_FAILURES plan(s) failed registration. Review errors above."
      fi
      fi  # close GSD_P_AUTO_TASK inner guard
    else
      # Legacy thin registration for phases 9-13 (grandfathered)
    ```

    Then WRAP the existing thin registration loop (lines 120-168) inside the `else` branch of this conditional (it becomes the legacy path). Close the `fi` after the existing loop ends (after line 168).

    **Kill switch two-layer check:** The outer `if` checks the phase number. For phase >= 14: if `GSD_P_AUTO_TASK=false`, skip task registration entirely with a log message (do NOT fall through to the legacy loop -- legacy loop is only for phases 9-13). The inner `planToTasks()` function also checks the kill switch (defense-in-depth). For phase < 14: fall through to the legacy thin registration loop (which has its own guards).

    **Part B: execute-phase.md — migration note**

    Add a note near the existing manifest-check migration note (around line 334):
    ```
    # plan-to-tasks migration note (Phase 14):
    # Phases 14+ use plan-to-tasks for full task registration.
    # Phases 9-13 use the legacy thin registration loop.
    # The phase number cutoff is authoritative — a <story> block in a
    # Phase 12 plan does NOT trigger registration.
    # Kill switch: GSD_P_AUTO_TASK=false disables plan-to-tasks everywhere.
    ```

    **Part C: .planning/STATE.md — cutoff documentation**

    In the "Roadmap Evolution" section (after the existing Phase 13.1 insertion note), add:
    ```
    - Phase 14+ plans are auto-registered via plan-to-tasks. Phases 9-13 are grandfathered. The presence of a `<story>` block is not sufficient to trigger registration — the phase number cutoff is authoritative. Kill switch: `GSD_P_AUTO_TASK=false`.
    ```

    **Part D: .planning/REQUIREMENTS.md — PLAN-04 errata (closeout step)**

    Find the PLAN-04 requirement text that describes implicit N+1 task ordering / default dependency relationship. Apply the errata per 14-CONTEXT.md Area 2:

    1. Strike through the original implicit-N+1 ordering text by wrapping it in `~~...~~` markdown strikethrough.
    2. Below the strikethrough, add the replacement text: "Dependencies are **explicit-only**. Every dependency MUST appear in `<depends_on>`. Tasks without explicit dependencies are parallel-eligible by default. NO implicit N+1 ordering."
    3. Add a footnote: `*Errata (Phase 14): Original implicit-N+1 text struck per PITFALLS P8 ("reject spurious dependency inference"). See 14-CONTEXT.md Area 2.*`

    This is the closeout-commit paperwork item from 14-CONTEXT.md. It ships with 14-04-03 rather than a separate task to stay within the 10-task cap.
  </action>
  <acceptance_criteria>
    - grep -c 'plan-to-tasks' get-shit-done/workflows/execute-phase.md returns at least 5 (invocation + comments + migration note)
    - grep 'PHASE_NUM_FLOAT.*>= 14\|float.*>= 14' get-shit-done/workflows/execute-phase.md returns a match (phase cutoff check)
    - grep 'GSD_P_AUTO_TASK' get-shit-done/workflows/execute-phase.md returns at least 2 (kill switch check + migration note)
    - grep 'Legacy thin registration\|legacy.*registration\|grandfathered' get-shit-done/workflows/execute-phase.md returns at least 1 (legacy path label)
    - The existing thin registration loop (lines ~120-168 content including $AMAUTA_CLI exec add task) is still present inside the else branch (verify with grep)
    - grep 'plan-to-tasks' .planning/STATE.md returns a match (cutoff documentation line)
    - grep 'phase number cutoff is authoritative' .planning/STATE.md returns a match
    - grep 'GSD_P_AUTO_TASK=false' .planning/STATE.md returns a match
    - grep 'explicit-only' .planning/REQUIREMENTS.md returns a match (PLAN-04 errata replacement text)
    - grep 'PITFALLS P8' .planning/REQUIREMENTS.md returns a match (errata footnote)
    - grep '~~' .planning/REQUIREMENTS.md returns a match (strikethrough of original implicit-N+1 text)
  </acceptance_criteria>
  <files_expected>
    modify:
      - get-shit-done/workflows/execute-phase.md
      - .planning/STATE.md
      - .planning/REQUIREMENTS.md
    create: []
    delete: []
  </files_expected>
</task>
